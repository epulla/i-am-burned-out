---
description: Review the current diff for over-engineering and slop; return a delete-list
---

Load the `burnedout-review` skill and run it. Use `$ARGUMENTS` as the review base when it is non-empty.
