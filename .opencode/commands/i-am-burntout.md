---
description: Set the burntout level (lite | full | ultra | off) or report the current one
---

Load the `i-am-burntout` skill and apply it at level `$ARGUMENTS` for the rest of this conversation. No argument reports the current level without changing it; report `full` if no level has been set. `off` restores normal behavior.

Reply with one line only: `burntout: <level>`.
