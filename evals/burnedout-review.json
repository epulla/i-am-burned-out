{
  "skill_name": "burnedout-review",
  "evals": [
    {
      "id": 1,
      "prompt": "Run burnedout-review on this change: fixtures/retry-client.test.ts was just added for fetchWithRetry.",
      "expected_output": "Numbered delete-list with file:line items for: the `is a function` test (tests the language), the `calls fetch` test (asserts a mock was called), the `b.test` case (repeats `a.test` with a different literal), and `it.todo`. Keeps `retries after a failure` and `throws after three failures`, which fail if the retry loop is reverted. Does not flag `makeResponse`, which four tests share. No style or naming comments, no edits applied.",
      "files": ["fixtures/retry-client.ts", "fixtures/retry-client.test.ts"]
    },
    {
      "id": 2,
      "prompt": "Run burnedout-review. The only change is fixtures/retry-client.ts, and nothing in it can go.",
      "expected_output": "Replies exactly `burnedout: nothing to cut`.",
      "files": ["fixtures/retry-client.ts"]
    },
    {
      "id": 3,
      "prompt": "Run burnedout-review on this branch. Ticket SERV-2238: optional document download failures no longer fail delivery. git diff --stat main...HEAD shows 22 files, +1,400/-60: missing_required_documents() in download.py, is_permanent_http_error and fail_fast_on_permanent in cache_downloads.py, document_service_hook.py now raises HTTPError, new AirflowFailException and unrecoverable_types, slack_channels passed through 4 task files and 2 utils files, legacy api_deliveries/* path, and about 1,000 test lines.",
      "expected_output": "Opens with scope groups before any line-level item: retry policy (cache_downloads.py, hook HTTPError change, their tests), failure mode (AirflowFailException, unrecoverable_types, tests), alerting (slack_channels through 6 files), legacy path (api_deliveries/*), each with a one-line follow-up. Flags the no-retry-on-4xx behavior change as unrequested. Reports current: 22 files +1,400/-60 and an in-scope estimate near 1-2 files. Keeps missing_required_documents() and its test.",
      "files": []
    }
  ]
}
