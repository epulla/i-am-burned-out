{
  "skill_name": "i-am-burned-out",
  "evals": [
    {
      "id": 1,
      "prompt": "My Node script throws ECONNREFUSED when it tries to hit localhost:5432. What's wrong?",
      "expected_output": "Opens with the cause (nothing listening on 5432 / Postgres not running), one or two checks, one next step. No opener, no closer, no bullet list of five possibilities.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Add a color picker to the settings form in this React app.",
      "expected_output": "Reaches for <input type=\"color\"> (rung 4) instead of installing a library. Keeps any existing form validation. No new hook, constant, or wrapper component. Leaves an `// burnedout:` marker if it skips something the user might expect.",
      "files": ["fixtures/settings-form/SettingsForm.tsx"]
    },
    {
      "id": 3,
      "prompt": "Explain the difference between git rebase and git merge.",
      "expected_output": "Direct explanation, normal English, under ~120 words, no pros/cons table, no 'It's worth noting', no summary paragraph.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Find where the retry logic lives in this repo and show me the relevant lines.",
      "expected_output": "Locates matching filenames first, then reads only relevant matching lines from the specific file. Does not dump the repository, run an unbounded recursive search, or print the whole file.",
      "files": ["fixtures/retry-client.ts"]
    },
    {
      "id": 5,
      "prompt": "Rename the `name` field to `fullName` in the settings form, update the validation and every reference, and add a test for the new field name.",
      "expected_output": "Creates a todo list before editing (3+ steps): one line per item, at most 5. Exactly one item in_progress at a time, each marked done as it finishes, no narration such as 'marking item 1 complete'. Ends with one next step.",
      "files": ["fixtures/settings-form/SettingsForm.tsx"]
    },
    {
      "id": 6,
      "prompt": "Delegate to a subagent: find every call site of `fetchWithRetry` and report back.",
      "expected_output": "The delegated prompt restates the binding constraints and requires terse output: findings only, file:line references, no narration, no restatement of the brief. The main reply relays the findings without re-narrating the subagent's work.",
      "files": ["fixtures/retry-client.ts"]
    },
    {
      "id": 7,
      "prompt": "Write tests for fetchWithRetry.",
      "expected_output": "Two or three tests: success after a transient failure, rethrow after the third failure, and optionally first-try success. Stubs only global fetch (the network boundary) and asserts returned responses, thrown errors, and attempt counts where they are the behavior. No tests for the URL type, no duplicated cases with different URLs, no skip/todo, no shared fixture or helper used once.",
      "files": ["fixtures/retry-client.ts"]
    },
    {
      "id": 8,
      "prompt": "What version is this package on?",
      "expected_output": "Runs one command that returns one value, such as `jq -r .version package.json` or `grep -m1 '\"version\"' package.json`, and replies with the version. Does not cat package.json, list the directory, or search the whole repository.",
      "files": []
    },
    {
      "id": 9,
      "prompt": "Run the tests for the retry client and tell me what failed.",
      "expected_output": "Runs the test command filtered to the retry client with a quiet reporter and fail-fast flag, redirects the full log to a temp file, and reports only failing test names, the first error message, and the exit code. Does not paste the full suite output or rerun the same broad command to read one value.",
      "files": ["fixtures/retry-client.ts", "fixtures/retry-client.test.ts"]
    },
    {
      "id": 10,
      "prompt": "Add a maximum length to the name field in the settings form.",
      "expected_output": "Either picks a limit, states it as an assumption in one line, and makes the change, or asks exactly one question with the default it will use if unanswered. Does not ask multiple questions or stop without a default.",
      "files": ["fixtures/settings-form/SettingsForm.tsx"]
    },
    {
      "id": 11,
      "prompt": "Review comment on my PR: \"create a document class for each of the 7 new PDF types so they go through Pdf.parse\". Plan the change; the base class _UnparsedPdf has is_valid = False and a PARSERS dict maps type to class.",
      "expected_output": "States the change as one sentence (the 7 types parse through Pdf.parse). Proposes changing the is_valid default or one fallback branch in the existing base class instead of 7 classes, names the skipped shape in one line, keeps PARSERS as is, and plans one parametrized test. Makes no edits before the user says go.",
      "files": []
    }
  ]
}
